// Generated by CoffeeScript 1.9.3
(function() {
  var REDIS, RedisClient, redis;

  redis = require('redis');

  REDIS = nodame.config('cache.db.redis');

  RedisClient = (function() {
    function RedisClient() {
      this._client = void 0;
      return;
    }

    RedisClient.prototype.client = function() {
      if (this._client == null) {
        this._client = redis.createClient(REDIS.port, REDIS.host, {});
        this._client.on('error', this._errorHandler);
      }
      return this._client;
    };

    RedisClient.prototype._errorHandler = function(err) {
      return console.log("Error Redis " + err);
    };

    return RedisClient;

  })();

  module.exports = new RedisClient();

}).call(this);
