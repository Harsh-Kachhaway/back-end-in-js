// Generated by CoffeeScript 1.9.3

/*
 * @author  Argi Karunia <arugikaru@yahoo.co.jp>
 * @link    https://github.com/tokopedia/nodame
 * @license http://opensource.org/licenses/MIT
 *
 * @version 1.0.0
 */

(function() {
  var Strings;

  Strings = (function() {
    function Strings() {}

    Strings.prototype._trim = function(str, middle) {
      str = str.replace(/[ ]+/g, ' ');
      if (middle) {
        str = str.replace(/^[ ]+|[ ]+$/g, '');
      }
      return str;
    };

    Strings.prototype.trim = function(data, middle) {
      var _tmp, dataIsArray, dataIsString, key, temp;
      if (typeof data === 'object') {
        dataIsArray = Object.prototype.toString.call(data) === '[object Array]';
        temp = dataIsArray ? [] : {};
        for (key in data) {
          if (data.hasOwnProperty(key)) {
            dataIsString = typeof data[key] === 'string';
            if (dataIsString) {
              _tmp = this._trim(data[key], middle);
            } else {
              _tmp = data[key];
            }
            if (dataIsString) {
              if (dataIsArray) {
                temp.push(_tmp);
              } else {
                temp[key] = _tmp;
              }
            } else {
              if (dataIsArray) {
                temp.push(_tmp);
              } else {
                temp[key] = _tmp;
              }
            }
          }
        }
        return temp;
      } else {
        if (typeof data === 'string') {
          return this._trim(data, middle);
        } else {
          return data;
        }
      }
    };

    return Strings;

  })();

  module.exports = new Strings();

}).call(this);
